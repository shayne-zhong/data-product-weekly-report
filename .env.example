# Choose one provider in /admin and configure the corresponding key in Netlify.
DEEPSEEK_API_KEY=
MOONSHOT_API_KEY=
AI_DAILY_LIMIT=30

# Existing application sync key. Keep the production value in Netlify only.
REPORT_SYNC_KEY=
