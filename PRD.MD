# 数据产品部工作台 PRD

## 指标关联待办与待办产物

1. 指标贡献仅来源于关联待办；待办状态为“已完成”时，其贡献数才计入指标，多个已完成待办的贡献数累加形成指标当前值。
2. 指标关联、贡献数、贡献说明和产物只能在待办详情中新增或变更；目标页的“关联待办事项详情”只能查看。
3. 每个待办可关联多个指标，并可拥有一个当前产物。产物支持 Word、Excel、PPT、PDF、图片和 HTML，单文件不超过 20 MB。
4. 图片、PDF、HTML直接预览；Word、Excel、PPT由 LibreOffice 转为 PDF 后预览。
5. 待办产物可在待办详情上传、替换、下载或删除；替换失败时必须保留旧产物。
6. 关联待办事项详情展示待办状态、贡献数、贡献说明及产物查看入口，但不提供关联或产物编辑操作。
7. 目标页不再展示产物列；“操作”列改名为“关联待办”，并移除拆解和行内删除按钮。
8. “新增目标”旁提供“删除目标”按钮。点击后进入选择态，支持单选或批量删除；删除目标只解除待办关联，不删除待办。
9. 删除“恢复初始目标”按钮。当前没有历史目标产物，无需迁移旧产物数据。

> 当前版本：2026-08-29
> 本文档是产品需求的唯一基线。新增或变更产品需求在确认后必须先更新本文档，再进入开发。

## 产品定位

部门工作台用于连接部门目标、执行任务、进展记录与周期总结，减少周报、月报和季报的重复整理工作，并保留结果对应的目标、任务和指标来源。

系统应支持复制给不同部门使用。“数据产品部”是一个可配置部门实例，不应作为系统唯一固定部门。

## 核心目标链路

年度目标 → 季度目标 → 月度重点 → 周任务 → 每日进展 → 周报 / 月报 / 季报。

## 核心原则

1. 目标、任务、进展和总结之间应可追溯。
2. 周期总结先生成结构化初稿，再由负责人复核和补充。
3. 自动生成内容必须展示来源，不将推测内容直接作为事实。
4. 归档后的总结不可编辑，避免历史口径被覆盖。
5. 每项新需求应包含明确的验收标准后再进入开发。
6. AI 生成结果只作为候选内容，必须由用户复核，不得自动覆盖原始周报。
7. 第三方 AI 密钥不得暴露给浏览器或以明文写入业务数据。

## 用户与后台管理

### 任务权限

1. 部门成员分为普通用户、模块负责人、部门负责人三类。普通用户仅可访问负责人账号为本人的任务；模块负责人可访问本人任务及其负责模块内的全部任务；部门负责人可访问本部门全部任务。
2. 任务访问范围统一适用于列表、详情、创建、编辑、删除、状态更新、负责人转交及附件上传、预览、下载和删除。无权限访问任务时不得泄露任务是否存在。
3. 一个模块负责人可负责多个模块。普通用户只能创建或转交给自己的任务；模块负责人只能在所负责模块内操作并可指定本部门成员；部门负责人可操作本部门任意任务。
4. 后台维护人员角色和模块负责人范围。部门负责人可维护本部门普通用户和模块负责人；全局管理员还可指定或更换部门负责人。每个部门同一时刻仅允许一名部门负责人。
5. 任务负责人必须以账号标识保存，同时保留姓名用于展示。历史任务按负责人姓名唯一匹配账号；未匹配或重名任务不向普通用户开放，由模块负责人按模块或部门负责人处理。

### 部门管理

1. 后台支持维护部门。
2. 页面标题、周报标题和相关展示信息应使用当前部门配置。
3. 系统能力不与“数据产品部”硬编码绑定，便于复制给其他部门使用。

### 登录与会话

1. 后台支持配置用户保持登录的时间。
2. 管理后台及 AI 调用能力必须经过登录校验。
3. 管理员凭证和会话密钥由服务端安全配置，不写入前端代码。

### WorkBuddy 与企微原生待办集成

#### 范围与职责

1. 第一阶段仅为数据产品部启用集成；实现通过配置绑定部门，不将部门名称硬编码为系统唯一部门。
2. 员工主要在部门工作台创建、修改和转交任务；WorkBuddy 读取任务增量并维护企微原生待办。
3. 员工在企微原生待办中勾选完成后，WorkBuddy 可调用状态回写 API 将网站任务标记为已完成。
4. 本期只实现增量任务查询、完成状态回写、账号与企微 `userid` 映射、OAuth 免登回调四项，不实现 Webhook、游标分页、删除同步或其他企微反向编辑。

#### 增量任务查询

1. 提供 `GET /api/open/tasks?updated_since=`，仅接受独立的部门级 Bearer Token，不复用员工会话或 OAuth 凭证。
2. `updated_since` 和返回的 `updated_at` 均为非负 Unix 秒整数；查询返回 `updated_at > updated_since` 的当前任务，并按 `updated_at` 升序排列。
3. 每条任务返回 `task_id`、`title`、`description`、`assignee_userid`、`status`、`due_date` 和 `updated_at`。
4. 系统为开放接口维护严格递增的秒级更新时间；同一秒发生多次任务变化时仍不得产生相同的 `updated_at`。
5. 新建、标题或描述修改、负责人转交、状态变化、截止日期变化及负责人企微映射变化，都必须推进对应任务的开放接口更新时间。
6. 上线时为数据产品部现有任务初始化开放接口更新时间；初始化必须持久化防重。
7. 本期接口不提供分页、游标、变更事件、删除墓碑或已删除任务。

#### 完成状态回写

1. 提供 `PUT /api/open/tasks/:task_id/status`，使用与增量查询相同的部门级 Bearer Token。
2. 本期只允许把任务更新为已完成；首次成功回写返回 200，并推进任务的开放接口更新时间。
3. 已完成任务是终态；对终态再次回写返回 409，且不得重复修改任务、指标贡献或更新时间。
4. 状态回写必须继续执行工作台现有完成规则：任务未关联年度指标或未填写有效贡献数时不得完成，并返回可识别的 422 错误。
5. 不存在、已删除或不属于集成部门的任务返回 404，不泄露其他部门任务。
6. 回写成功时记录 WorkBuddy 集成身份为更新来源，不能伪造成普通员工操作。

#### 员工标识映射

1. 在现有网站账号目录中增加 `wecom_userid` 字段，并保持网站账号与企微 `userid` 一对一唯一。
2. 首次接入通过 WorkBuddy 提供的企微通讯录结果初始化映射；初始化只接受数据产品部账号，并按网站账号标识精确匹配，不按姓名猜测。
3. OAuth 首次登录作为兜底：尚未绑定时，只有 WorkBuddy 同时返回可与网站账号唯一匹配的账号标识，系统才自动回填 `wecom_userid`。
4. 若网站账号或企微 `userid` 已绑定其他对象、账号不存在、账号标识不唯一或部门不一致，则拒绝自动回填。
5. 负责人尚未绑定时，增量任务的 `assignee_userid` 返回 `null`；映射补齐后推进该负责人当前任务的开放接口更新时间。
6. 普通员工不能查看或修改他人的企微标识；开放任务接口只向已授权的 WorkBuddy 返回任务所需映射。

#### 企微 OAuth 免登

1. 网站提供 `/wecom/callback`，接收企微授权 `code` 与一次性 `state`。
2. 网站先验证 `state`，再由服务端将一次性 `code` 交给 WorkBuddy 身份解析接口；企微应用密钥由 WorkBuddy 保存，不进入网站前端或业务数据。
3. WorkBuddy 返回已验证的企微 `userid` 和网站账号标识后，系统优先查找现有映射；映射缺失时按员工标识映射规则自动回填。
4. 仅数据产品部内有效且已注册的网站账号可以建立现有网站会话；不得自动创建网站账号。
5. `code` 不进入浏览器存储、不写日志且只使用一次；`state` 五分钟失效并禁止重复使用，登录后的回跳地址必须经过白名单校验。
6. OAuth 登录与账号密码登录复用同一套后端任务权限；现有账号密码登录继续保留作为备用入口。

验收标准：

1. `GET /api/open/tasks?updated_since=` 只返回数据产品部且更新时间大于查询值的当前任务，并包含约定字段。
2. 连续任务变化获得严格递增的秒级 `updated_at`；WorkBuddy 保存最大值后能继续增量查询而不遗漏。
3. WorkBuddy 首次完成回写成功；重复回写终态返回 409，且任务和贡献数不重复变化。
4. 不满足现有年度指标贡献规则的任务回写完成时返回 422，任务保持未完成。
5. 通讯录初始化能建立唯一映射；OAuth 首登只在账号精确唯一匹配时自动回填，冲突时拒绝。
6. 未绑定负责人返回 `assignee_userid: null`；绑定后该负责人任务重新出现在增量结果中。
7. 错误令牌、伪造或重复 `state`、无效 OAuth code 和跨部门访问均被拒绝，不泄露其他部门任务。
8. WorkBuddy 身份解析不可用时，账号密码登录和现有任务功能保持可用。

### 后台入口

1. 顶部导航的“后台管理”和“指引”使用图标展示，不显示文字。
2. 图标必须保留可识别提示及无障碍说明。
3. 网页标题统一为“部门工作台”，并配置专用网站图标。

### 后台管理中心

1. 后台采用独立应用壳：64px 顶栏、216px 左侧分组导航、40px 可关闭页签和内容工作区；管理概览固定为默认页且不可关闭。窄屏下侧栏收起为图标导航，手机端使用抽屉导航。
2. 一级导航固定为“组织管理、规则配置、系统运维”。组织管理包含“部门管理、成员管理”，规则配置包含“保持登录、归档设置”，系统运维包含“API 密钥、定时任务”。
3. “部门管理”合并部门基础信息、负责人、启停状态和工作模块；“成员管理”合并成员账号、所属部门、角色、负责模块、启停和密码重置。
4. “项目类型”在界面中统一命名为“工作模块”；底层数据字段和历史数据保持兼容，不因改名执行数据迁移。
5. 全局管理员可访问全部导航。部门负责人只显示“组织管理”，可维护本部门工作模块及成员角色、负责模块、启停和密码，不可新增或删除成员账号。
6. 当前页面在侧栏和页签中同时高亮；侧栏打开页面时复用已有页签，关闭活动页签后回到相邻页，管理概览始终保留。
7. 每个二级页面独立保存配置。切换页面保留未保存草稿并标记对应菜单；保存失败保留输入并显示原因。
8. 清除 API 密钥和人工补跑继续要求显式确认，菜单可见性不替代服务端权限校验。
9. 离开后台管理视图或关闭页面前，存在未保存修改时必须二次确认。
10. 密码重置、账号停用、角色变更、API 密钥清除和人工补跑不得与普通配置批量保存，必须单独提交和确认。
11. 成员与任务类列表保留适用的关键词、部门、角色和启用状态筛选，并明确区分加载中、无数据、无匹配、无权限和加载失败状态。
12. 后台包含管理概览及现有六个管理页面；视觉和交互以 `docs/design/admin/DESIGN.md` 为后台专用规范，统一 36px 控件、4px 控件圆角、白色内容容器、品牌渐变主操作与清晰的危险操作层级。

### 后台数据看板

1. 管理概览是后台默认首页，以“待处理事项优先”为原则，不建设装饰性大屏。
2. 全局管理员可查看全部部门或指定部门；部门负责人仅可查看本部门。服务端必须在统计前应用角色范围，不得依赖前端过滤。
3. 看板支持按本周、本月、本季度查看，并展示最后更新时间及手动刷新入口。
4. 看板包含组织概况、任务执行、总结归档、系统健康和待处理事项；首版仅使用现有业务数据，不增加预测指标。
5. 指标必须展示数值、口径说明和异常状态；无有效分母时显示“—”，不得显示误导性的 0%。
6. 指标或异常项可下钻到对应管理页或业务页，并保留部门、周期和状态筛选条件。
7. 待处理事项至少包括定时任务失败、缺少部门负责人、模块负责人未配置工作模块、历史任务负责人无法唯一匹配以及应归档但未归档的总结。

### 后台角色保护

1. 部门负责人不得指定、替换或停用部门负责人；该能力仅限全局管理员。
2. 每个部门同一时刻最多一名部门负责人。更换时必须明确原负责人的新角色后一次性完成变更。
3. 模块负责人至少关联一个工作模块；否则标记为配置异常并进入待处理事项。
4. 仍被指定为部门负责人的成员账号不得直接停用，必须先完成负责人更换；全局管理员凭证不纳入普通成员账号启停。
5. 后台菜单、列表、详情、统计和修改接口必须使用一致的服务端权限策略。

### 后台操作审计

1. 后台记录角色变更、账号启停、密码重置、部门负责人更换、AI 密钥配置或清除、登录策略修改、归档规则修改和人工补跑结果。
2. 审计记录包含操作者、时间、部门、操作类型、影响对象、成功或失败以及不含敏感信息的变更摘要。
3. 审计记录不得包含密码、完整密钥、会话令牌、请求头或错误堆栈。
4. 首版保留最近 180 天且最多 5,000 条记录，任一上限达到后清理最旧记录。

### 定时任务管理

1. 运行中心提供定时任务和执行记录入口，展示任务名称、计划状态、计划时间、最近执行状态、执行时间、执行来源、源周期、目标周期、业务结果和失败原因。
2. 当前管理对象为“周任务定时结转”；执行状态至少区分“从未执行”“执行中”“成功”“失败”。
3. 仅全局管理员可查看定时任务状态并发起人工补跑；部门负责人和普通用户无权访问。
4. 人工补跑沿用“部门 + 源周 + 目标周”的持久化防重机制，已成功的部门不得重复创建任务。
5. 调度入口在开始执行前写入“执行中”状态，成功后写入结果摘要；失败时写入经过截断的错误摘要，不保存密钥、请求头或堆栈等敏感信息。
6. 后台补跑必须由管理员显式点击触发，不自动无限重试；单次点击只执行一次，并在完成后刷新状态。
7. 人工操作命名为“检查并补跑”，确认文案必须说明只处理已到期但尚未完成的内容，不会重复创建或提前归档。
8. 执行来源至少区分“自动调度”“启动补偿”“管理员手动运行”；业务结果至少展示检查范围、成功数量、跳过数量和失败数量。

验收标准：

1. 管理员可看到周任务结转的计划时间及最近一次成功或失败详情。
2. 调度执行失败后，后台显示失败状态和可读错误摘要。
3. 管理员点击“重新运行”后可安全补跑；重复补跑已成功周次时新增任务数为 0。
4. 未登录、普通用户和部门负责人访问定时任务管理接口时返回无权限。
5. 后台不会展示调度密钥或其他服务端敏感配置。

## 周报功能

### 周报填写

1. 用户可直接在网页中填写周重点工作。
2. 周报至少支持汇报周期、工作分类、本周进展、当前风险和下周重点。
3. 支持呈现需要管理层关注或决策的事项。

### 周报复制

1. 支持将填写内容整理为结构化文本并一键复制。
2. 原始文本复制能力必须独立可用，不依赖 AI 服务。
3. 复制结果应保留部门、汇报周期、分类、状态及各内容分区。

## AI 周报提炼

### 目标

在原始周报基础上使用第三方大模型总结、提炼重点或润色，降低人工整理成本。

### 供应商与配置

1. 支持 DeepSeek 和 Kimi 开放平台接口。
2. 后台可配置启用状态、供应商和模型。
3. 后台可安全配置或更新 API Key，但设置查询不得返回密钥明文。
4. AI 密钥由服务端加密保存；专用加密密钥缺失时，可使用后台会话密钥派生加密密钥。

### 生成与复核

1. 支持管理层重点版、高度精炼版和完整润色版。
2. AI 结果在独立预览中展示，不修改、不保存、不归档原始周报。
3. 用户可复制 AI 结果或重新生成。
4. 页面展示实际供应商、模型、Token 用量和当日剩余次数。
5. AI 不可用或调用失败时，原始周报填写与复制功能保持可用。
6. “复制 AI 结果”和“复制原始文本”必须直接写入系统剪贴板，并明确反馈成功或失败。

### 成本与安全限制

1. 仅登录用户可调用 AI。
2. 单次输入上限为 50,000 字。
3. 单次请求超时为 45 秒。
4. 服务端不自动重试，避免异常情况下重复消耗额度。
5. 每名用户默认每天最多调用 30 次，并支持通过部署配置调整。
6. 原始周报内容不因 AI 调用自动持久化。

## 月度与季度总结设计方向

月度总结和季度总结不再只是手工文本，而是基于目标、任务、贡献数自动形成初稿，负责人只需要复核和补充判断。

### 季度总结

季度总结建议围绕季度目标管理：

1. 季度目标
   从年度目标中选择本季度要推进的指标，设定季度目标值、责任人、关键里程碑。

2. 本季进展
   自动汇总本季度已完成任务、任务贡献数、指标当前值变化、重点成果。

3. 下季计划
   必须由负责人结合业务判断人工填写，不允许从月报、任务或 AI 结果自动提炼、导入或覆盖。

4. 当前风险
   汇总阻塞任务、逾期任务、指标滞后项，并支持负责人补充管理动作。

### 月度总结

月度总结建议承接季度目标：

1. 本月目标
   从季度目标拆解出本月目标，明确目标值、负责人、交付物。

2. 本月进展
   自动汇总本月任务完成情况、年度/季度指标贡献、关键上线或交付事项。

3. 下月计划
   必须由负责人结合业务判断人工填写，不允许从周报、任务或 AI 结果自动提炼、导入或覆盖。

4. 当前风险
   汇总阻塞任务、逾期任务、指标异常，并支持补充人工判断。

### 交互原则

1. 月报和季报应先生成结构化初稿，再允许人工编辑。
2. 所有总结都应保留来源：来源任务、来源目标、贡献数。
3. 归档后不可编辑，避免历史口径被覆盖。
4. 总结页面应提供“生成初稿”“复制文本”和“临时保存”；归档由后台定时任务完成。
5. 目标拆解页面后续应新增季度目标视图，避免季度/月度总结仅依赖临时文本。
6. 月度总结支持 AI 总结，默认汇总当前月份内所有已保存的周报（含草稿并明确标注状态），结构化生成“本月目标、本月进展、当前风险”。
7. 季度总结支持 AI 总结，默认汇总当前季度内所有已保存的月度总结（含草稿并明确标注状态），结构化生成“本季目标、本季进展、当前风险”。
8. AI 总结先在独立弹窗预览；仅在用户确认后写入当前总结，不能直接覆盖编辑区。
9. AI 总结不得生成或写入“下月计划/下季计划”；两个计划区不提供任务导入入口，必须人工填写。

## 部门待办四象限模式

### 周任务定时结转

1. 浏览器加载或刷新某一周时只读取该周任务，不触发跨周任务结转。
2. Node 服务在北京时间每日 08:00–20:00 的整点每小时检查一次；进入新周后的首次检查处理上一周任务，将“待开始”“进行中”“阻塞”任务结转到新一周；已完成任务不结转。
3. 周结转按“部门 + 源周 + 目标周”记录持久化执行结果。定时器重复投递、函数重试或人工补跑时，不得重复创建任务。
4. 结转任务保留 `sourceTaskId` 和 `sourceWeekId`，便于追踪来源；用户在本周删除结转任务后，页面刷新不得再次创建该任务。
5. 单次执行失败时不写成功标记，允许安全重试；成功记录需包含执行时间、源周、目标周、部门和结转数量。
6. Node 服务是唯一自动调度器：启动后必须补偿执行一次当前周结转，并在进程持续运行时按北京时间每日 08:00–20:00 整点每小时检查；Vercel 只提供页面和 API，不执行后台定时任务。

验收标准：

1. 页面刷新不产生结转写请求。
2. 删除结转任务后连续刷新不恢复。
3. 同一周结转任务重复执行时，第二次返回已处理且新增数量为 0。
4. 每个部门独立结转，某部门已有目标周副本时不重复创建。
5. Node 服务重启后能补偿缺失的本周结转；服务持续运行时在白天每小时检查，重复检查不会重复创建任务。

### 使用角色与入口

登录用户从“部门待办”进入。页面首次默认显示四象限模式，并可切换到原有列表模式；系统按当前用户和部门记住最后选择。

### 正常流程

1. 四象限按照重要紧急、重要不紧急、不重要紧急、不重要不紧急展示当前筛选范围内的未完成任务。
2. 已完成任务不在四象限展示，继续保留在列表模式和历史任务中。
3. 用户将任务拖入另一象限后，系统只修改优先级，不修改任务状态。
4. 每个象限可直接新增“待开始”任务，并自动带入当前象限优先级。
5. 用户可拖动中心点调整横纵比例，范围为 25%–75%；布局按用户和部门保存在浏览器中，可恢复为 50% × 50%。
6. 搜索、模块筛选、“只看我的”和批量管理同时作用于两种模式。

### 异常与边界

1. 优先级保存失败时任务恢复到原象限并提示失败。
2. 本地模式或布局数据损坏时恢复为四象限和 50% × 50%。
3. 窄屏下四象限纵向排列并隐藏中心拖动点。
4. 列表模式原有按状态拖动、阻塞原因校验和历史任务行为不变。

### 验收标准

1. 首次进入默认四象限，刷新后保留用户最后选择的模式和布局。
2. 四种优先级的未完成任务进入正确象限，已完成任务不出现。
3. 跨象限拖动成功后刷新仍保持分类，保存失败时自动回滚。
4. 中心点可在 25%–75% 范围内鼠标和键盘移动，恢复按钮回到 50% × 50%。
5. 筛选、新增和批量删除在四象限模式正常工作，列表模式无回归。

## 待办、目标与周报体验优化

### 部门待办

1. 四象限模式中的逾期任务使用明显的警示视觉和“已逾期”标识，提醒用户优先处理。
2. 进入列表模式时，系统自动将未完成且已逾期的任务状态持久化为“阻塞”。
3. 自动转为阻塞时追加“任务已逾期（原计划完成日期：YYYY-MM-DD）”，并保留已有风险或阻塞内容；重复进入不得重复追加。
4. 列表模式和四象限模式中的任务描述、当前风险/阻塞均最多展示两行，溢出内容以省略号收起，完整内容继续在任务详情中查看。

### 部门目标

1. 年度目标表格的所有业务列均允许用户拖拽调整宽度；选择列仅在删除目标选择态显示且不参与列宽设置。
2. 列宽只需保存在当前浏览器本地；刷新后恢复，数据缺失或损坏时使用默认列宽。
3. "预计达成"由用户手工维护，用于计算完成度；"实际达成"由已完成关联待办的贡献数派生，目标页不能直接编辑。
4. 完成度按"预计达成中的首个有效数字 ÷ 目标值中的首个有效数字"计算；预计达成为空、无法解析或目标值无有效正数时，完成度为 0%。
5. 平均完成度及没有手工状态时的自动状态判断均使用预计完成度。
6. "关联待办"列默认宽度 120px，允许在 100px 至 180px 之间调整。

### 周报管理

1. 左侧总结列表不再显示“打开”按钮，点击列表项本身即可打开。
2. 未选中的列表项不显示删除按钮，只有当前选中的总结显示删除按钮。
3. 取消“差异预览”和“保存并归档”操作；用户只负责保存草稿，归档由后台定时任务执行。
4. 后台可分别设置周报、月报和季报的归档时间，默认时区为北京时间：周报每周日 20:00，月报每月最后一天 20:00，季报每季度最后一天 20:00。
5. 定时任务仅归档对应周期内已存在且尚未归档的总结，以服务器最后一次成功保存的版本为准；不自动生成或补写内容。
6. 定时归档必须按“部门 + 总结 + 计划归档时刻”幂等执行，并支持服务重启后的漏跑补偿。
7. 内容来源必须与当前总结类型和周期一致：周报展示周期内真实任务，月报仅使用周期内已归档周报，季报仅使用周期内已归档月报。
8. 来源区域展示真实来源名称、周期、状态和数量；来源不足时明确提示，不使用当前任务总数代替。
9. 后台“定时任务”提供报告自动归档的人工补跑入口，仅全局管理员可操作。
10. 人工补跑与自动归档共用同一套到期判断，只归档已经到期且尚未归档的报告，不允许提前强制归档。
11. 补跑结果展示最近执行状态、触发方式、完成时间、归档数量和简短错误；重复补跑不得重复归档。
12. Node 服务是报告自动归档的唯一自动调度器：启动时补偿检查一次，并在北京时间每日 08:00–20:00 的整点每小时检查；Vercel 不执行后台定时任务。

## 功能范围

### 已确认需求

- 年度、季度、月度目标的逐级拆解。
- 周任务与每日进展承接目标。
- 根据目标、任务和贡献数生成周报、月报、季报初稿。
- 在网页中直接填写周报并复制为结构化文本。
- 使用 DeepSeek 或 Kimi 对周报进行提炼、总结和润色。
- AI 结果独立预览并由用户人工复核，不覆盖原始周报。
- 后台维护 AI 供应商、模型、启用状态和密钥。
- 系统支持多部门复用，后台可维护部门。
- 后台可配置用户保持登录时间。
- 月报和季报支持人工复核与补充。
- 总结内容保留来源。
- 总结支持复制文本和后台定时归档，不提供手动归档或差异预览。
- 归档后不可编辑。
- 汇总阻塞、逾期和指标滞后等风险。
- 数据产品部通过 WorkBuddy 增量读取工作台任务、回写企微完成状态，并支持企微账号映射与 OAuth 免登。

### 待确认需求

- 用户角色和权限边界。
- 目标、任务、指标和总结的数据模型。
- 审批或复核流程。
- WorkBuddy 以外的通知与提醒机制。
- 多人协作和修改记录。
- 多部门之间的数据隔离规则。
- AI 调用额度按用户、部门或全局统计的最终口径。
- 生产环境的长期部署平台与数据存储方案。

## 验收基线

每个功能需求进入开发前，至少补充：

1. 使用角色；
2. 触发入口；
3. 输入与输出；
4. 正常流程；
5. 异常与边界情况；
6. 可执行的验收标准。

## PRD 更新规则

1. 用户提出新的产品功能或产品行为变更时，先分析并确认需求。
2. 确认后先更新本 PRD，再进入编码。
3. Bug 定位、临时排查、部署操作、纯技术重构和不改变产品行为的修复，不作为新产品需求写入。
4. 未确认的想法只能放入“待确认需求”，不得写成已确认需求。
5. 每次更新在下方记录日期和摘要，不复制完整会话。

## 变更记录

- 2026-09-03：确认管理后台进行减法重设计；保留现有导航、数据与接口范围，统一东鹏企业后台视觉和交互层级，不新增独立概览或后台接口。
- 2026-09-03：确认采用 admin-redesign-v2 后台方案；改为侧栏与多页签应用壳，新增真实数据管理概览，并将后台设计规范与前台规范分离。
- 2026-08-29：按 WorkBuddy 对接方契约将本期范围收敛为增量任务查询、完成状态回写、通讯录与 OAuth 账号映射、OAuth 免登回调；删除 Webhook、游标和删除同步设计。
- 2026-08-07：确认目标"预计达成"与"实际达成"字段分离（expectedCurrent / current），完成度改用预计达成计算，历史 current 值恢复迁移，关联待办列宽调整为 100-180px。
- 2026-08-04：确认逾期任务提醒与自动阻塞、任务文本两行截断、目标列宽本地记忆、周报列表操作收敛、复制修复及月度/季度 AI 总结。
- 2026-08-03：确认部门待办四象限模式、跨象限修改优先级、用户级布局记忆及完成任务展示边界。
- 2026-07-27：根据前两次项目会话补充周报填写与复制、AI 周报提炼、多部门复用、后台部门维护、登录时长配置及界面品牌要求。
- 2026-07-27：补充产品定位、核心原则、功能范围、验收基线及 PRD 自动同步规则。
