# Codex 项目开发规范

## 核心目标

在保证正确性和开发效率的前提下，尽量降低 Codex（GPT-5.6）的 Token 消耗。默认采用“小范围读取、先分析确认、最小修改、独立验证、及时结束”的工作方式。

## 项目上下文入口

每个新任务开始时，必须先读取根目录 `PROJECT_ARCHITECTURE.md`，再根据任务关键词定向读取最少的实现文件。若架构文档缺失、明显过期或未覆盖当前区域，可以进行受限探索并在架构边界变化时同步更新该文档。

## 默认工作流程

每个开发任务分为两个阶段，未经用户确认不得跨阶段。

每个 Session 默认只处理一个明确目标。Bug 定位、功能开发和代码审查不得在同一 Session 中混合执行，除非用户明确要求。

新的产品功能或产品行为变更经用户确认后，必须先更新 `PRD.MD`，再进入编码。未确认内容只能标记为待确认；Bug 排查、临时操作、部署任务和不改变产品行为的技术修复不自动写入 PRD。

### 阶段一：分析

1. 先读取 `PROJECT_ARCHITECTURE.md`，不进行无目的的全项目扫描。
2. 用户明确指定文件、代码片段或路径时，以其为当前任务的优先范围。
3. 用户未指定文件时，根据任务关键词和架构文档选择最少的入口、模块或测试进行定向探索，无需先询问入口。
4. 只读取与任务直接相关的目录、配置或相邻文件；不读取依赖、生成物、备份或大型日志。
5. 先给出简洁的原因判断、修改范围、风险和验证方式。
6. 此阶段不写代码、不修改文件、不安装依赖、不执行有副作用的命令。
7. 等待用户明确确认后，再进入编码阶段。

### 阶段二：编码

1. 产品需求先同步 `PRD.MD`；纯技术任务可跳过。
2. 仅修改已确认范围内的必要代码。
3. 优先使用最小 Diff，不重写无关代码，不做顺手重构。
4. 不返回整个文件；只展示关键 Diff、修改点和验证结果。
5. 仅运行与改动直接相关的最小验证。
6. 验证失败时，先分析失败原因；同一路径最多自动重试 2 次，之后停止并请求用户决定。

## Token 与上下文预算

1. 默认每轮最多主动读取 3 个文件；超过前先征得用户同意。
2. 默认不读取单个文件中与问题无关的大段内容；优先读取指定行号、符号或搜索命中附近内容。
3. 默认回复控制在 300 字以内；复杂问题可适当增加，但不重复需求、上下文或已确认结论。
4. 不复述完整代码、日志和命令输出，只摘取定位问题所需的关键部分。
5. 已经确认的信息不再次询问；已经总结的信息不再次展开。
6. 若预计任务需要大量上下文，先建议拆成多个独立小任务或新 Session。
7. 首轮默认最多读取 2 个文件；读取第 3 个文件前必须说明原因。
8. 单次默认只读取相关函数、符号或命中位置附近 200 行以内。
9. 搜索必须限定路径和关键词，并限制返回结果数量。
10. 测试、构建和日志输出只保留摘要及错误附近的必要片段。

## 文件读取规则

1. `PROJECT_ARCHITECTURE.md` 是每个任务的固定必读文件，不计入用户指定的文件白名单。
2. 用户给出明确文件时，优先读取这些文件；确需额外上下文时，只扩大到架构文档指向的直接相关文件。
3. 用户未指定文件时，允许根据架构文档进行有目的的定向探索，不再要求先询问入口。
4. 禁止无目的地读取锁文件、生成物、构建目录、依赖目录、备份和大型日志。

## 修改与验证规则

1. 修改前明确影响范围，修改后核对实际 Diff。
2. 保留用户已有改动，不覆盖、不回滚无关内容。
3. 生成者不能自行判定结果必然正确；至少使用一项独立验证，例如相关测试、类型检查、静态检查或针对性运行结果。
4. 验证应与风险匹配；小改动不运行全项目测试，除非用户明确要求。
5. 涉及删除、迁移、发布、外部写入或不可逆操作时，必须单独获得确认。

## 任务模式

开始工作前先确定且只采用一种任务模式。具体提示词见 `TASK_TEMPLATES.md`。

### Bug 定位

1. 默认只分析原因，不修改代码。
2. 结论必须区分为“已确认”“高概率”和“待验证”。
3. 信息不足时，只申请最少的具体文件、函数或行号范围，并说明用途。

### Feature 开发

1. 先明确验收标准和影响范围。
2. 先提供最小实现方案，得到确认后再编码。
3. 不附带重构、格式化或额外优化。

### Review 审查

1. 默认只审查用户提供的 Diff。
2. 只报告可验证且会影响正确性、安全性或维护性的具体问题。
3. 如需上下文，只申请与问题直接相关的最少代码。

## 停止条件

遇到以下任一情况立即停止扩展工作，并向用户报告：

1. 连续两轮分析没有获得新证据。
2. 同一失败路径已自动重试 2 次。
3. 预计需要读取超过 5 个文件。
4. 需求或验收标准存在会改变实现方向的歧义。
5. 修改已通过约定验证；不得继续主动优化。
6. 当前任务需要跨越 Bug、Feature 或 Review 模式。

## 输出规范

分析阶段只输出：

- 原因或判断；
- 拟修改的文件和位置；
- 最小方案；
- 风险与验证方式；
- 等待确认。

编码阶段完成后只输出：

- 修改摘要；
- 关键 Diff 或文件链接；
- 验证结果；
- 尚存风险或下一步（如有）。

不要返回整个文件，不重复用户需求，不写冗长过程说明。

## 功能完成总结

每完成一个功能，自动生成不超过 5 行的开发总结：

1. 完成内容；
2. 修改文件；
3. 验证结果；
4. 未解决事项（没有则省略）；
5. 建议的新 Session 起始提示（仅在需要继续开发时提供）。

功能完成后立即结束当前任务，不主动扩展需求，不以长上下文继续下一个功能。后续功能优先开启新 Session，并仅携带上述总结和必要文件白名单。

交接内容使用 `SESSION_HANDOFF.md`，只保留当前状态，不复制完整对话、代码或日志。

## 用户指令优先级

用户在当前任务中的明确要求优先于本规范。若用户明确授权扫描、直接编码、扩大读取范围、运行完整测试或连续完成多个功能，可在该次任务内按授权执行。
