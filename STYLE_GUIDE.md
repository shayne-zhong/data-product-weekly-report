# 数据产品部工作台样式标准

## 设计定位

工作台是部门负责人用于目标牵引、任务执行、周报沉淀的管理工具。界面应保持高密度、清晰、稳定，优先支持快速扫描、编辑和复核。

## 视觉基准

- 背景：`#f5f7fb`
- 面板：白底、1px 浅边框、轻阴影
- 主色：`#315efb`
- 成功：`#16a36a`
- 风险：`#e58a00`
- 阻塞/滞后：`#e5484d`
- 圆角：页面卡片 14-16px，内部控件 9-10px，标签 7px

## 布局规范

- 顶部导航固定为：部门待办、部门目标、周报、个人中心、账号管理。
- 页面首屏统一使用 `.page-head`：左侧标题与说明，右侧操作按钮。
- 数据概览统一使用 `.summary-grid` + `.summary-card`。
- 表格统一使用 `.panel.table-wrap` + `.data-table`。
- 编辑弹窗统一使用 `.modal.wide`，表单使用 `.form-grid.task-detail-form`。

## 组件规范

- 主操作：`.primary-btn`
- 次操作：`.ghost-btn`
- 弱强调操作：`.soft-btn`
- 危险操作：`.danger-btn`
- 状态标签：`.status-pill`
- 任务卡片：标题、标签、截止日/负责人、描述、风险、进度条。

## 交互规范

- 编辑类数据默认自动保存，状态用顶部同步提示反馈。
- 任务进度统一为 0-100 的整数百分比。
- 日期输入统一使用浏览器原生 `type="date"`，云端保存为 `YYYY-MM-DD`。
- 负责人从账号管理清单中选择，避免自由输入导致统计失真。
